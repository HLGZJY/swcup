# 后端启动配置指南 — 鼻纹智救（NestJS 版）

> 整理人：统筹协调者
> 日期：2026-05-13
> 框架：NestJS + TypeScript + MySQL + TypeORM
> 端口：3000

---

## 一、技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 框架 | NestJS | 模块化、依赖注入、TypeScript |
| ORM | TypeORM | 数据库 ORM，支持 MySQL |
| 认证 | @nestjs/jwt + passport-jwt | JWT 鉴权 |
| 校验 | class-validator + class-transformer | DTO 自动校验 |
| 文档 | @nestjs/swagger | OpenAPI 自动生成（访问 /api-docs） |
| 日志 | nestjs-pino | 结构化日志 |
| 上传 | multer | 文件上传 |

---

## 二、项目初始化

### 2.1 创建项目

```bash
cd /mnt/f/swcup2026/backend

# 使用 NestJS CLI 创建项目（选 npm，禁用 strict 方便快速开发）
npx @nestjs/cli new . --package-manager npm --skip-git

# 安装核心依赖
npm install @nestjs/typeorm typeorm mysql2 @nestjs/jwt @nestjs/passport passport passport-jwt class-validator class-transformer @nestjs/swagger multer uuid

# 安装类型定义
npm install -D @types/passport-jwt @types/multer
```

### 2.2 目录结构

```
backend/src/
├── main.ts                      # 入口
├── app.module.ts                # 根模块
│
├── common/                      # 公共模块
│   ├── decorators/
│   │   └── roles.decorator.ts   # @Roles('admin') 装饰器
│   ├── guards/
│   │   ├── jwt-auth.guard.ts    # JWT 鉴权守卫
│   │   └── roles.guard.ts      # 角色校验守卫
│   ├── filters/
│   │   └── http-exception.filter.ts  # 全局异常过滤器
│   └── interceptors/
│       └── transform.interceptor.ts  # 统一响应格式拦截器
│
├── config/                      # 配置
│   └── configuration.ts         # dotenv 加载
│
├── auth/                        # 认证模块
│   ├── auth.module.ts
│   ├── auth.controller.ts
│   ├── auth.service.ts
│   ├── strategies/
│   │   └── jwt.strategy.ts
│   └── dto/
│       ├── login.dto.ts
│       └── register.dto.ts
│
├── users/                       # 用户模块
│   ├── users.module.ts
│   ├── users.service.ts
│   └── entities/
│       └── user.entity.ts
│
├── animals/                     # 动物档案模块
│   ├── animals.module.ts
│   ├── animals.controller.ts
│   ├── animals.service.ts
│   ├── dto/
│   │   ├── create-animal.dto.ts
│   │   └── query-animal.dto.ts
│   └── entities/
│       └── animal.entity.ts
│
├── nose/                        # 鼻纹模块
│   ├── nose.module.ts
│   ├── nose.controller.ts
│   ├── nose.service.ts
│   └── dto/
│       ├── collect.dto.ts
│       └── compare.dto.ts
│
├── events/                      # 救助事件模块
│   ├── events.module.ts
│   ├── events.controller.ts
│   ├── events.service.ts
│   ├── dto/
│   │   └── create-event.dto.ts
│   └── entities/
│       └── event.entity.ts
│
├── claims/                      # 认领模块
│   ├── claims.module.ts
│   ├── claims.controller.ts
│   ├── claims.service.ts
│   └── dto/
│       └── create-claim.dto.ts
│
└── admin/                       # 管理端模块
    ├── admin.module.ts
    ├── admin.controller.ts
    └── admin.service.ts

backend/.env                     # 环境变量（勿提交）
backend/.gitignore
backend/package.json
backend/tsconfig.json
backend/nest-cli.json
```

---

## 三、核心配置文件

### 3.1 环境变量 `.env`

```env
PORT=3000
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=nose_rescue
JWT_SECRET=your_jwt_secret_key_change_in_production
JWT_EXPIRES_IN=7d
```

### 3.2 `src/main.ts`（入口 + Swagger + 全局拦截器）

```typescript
import { NestFactory } from '@nestjs/core';
import { ValidationPipe } from '@nestjs/common';
import { SwaggerModule, DocumentBuilder } from '@nestjs/swagger';
import { AppModule } from './app.module';
import { ConfigService } from '@nestjs/config';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);

  // 获取 ConfigService
  const configService = app.get(ConfigService);
  const PORT = configService.get<number>('PORT') || 3000;

  // 全局校验管道
  app.useGlobalPipes(
    new ValidationPipe({
      whitelist: true,           // 去除不在 DTO 里的字段
      transform: true,           // 自动类型转换
      forbidNonWhitelisted: true // 拒绝未知字段
    })
  );

  // 全局异常过滤器
  app.useGlobalFilters(new HttpExceptionFilter());

  // CORS
  app.enableCors();

  // Swagger 文档
  const swaggerConfig = new DocumentBuilder()
    .setTitle('鼻纹智救 API')
    .setDescription('第十五届软件杯参赛项目 API 文档')
    .setVersion('1.0')
    .addBearerAuth()
    .build();
  const document = SwaggerModule.createDocument(app, swaggerConfig);
  SwaggerModule.setup('api-docs', app, document);

  await app.listen(PORT);
  console.log(`后端已启动: http://localhost:${PORT}`);
  console.log(`API 文档: http://localhost:${PORT}/api-docs`);
}
bootstrap();
```

### 3.3 `src/app.module.ts`（根模块）

```typescript
import { Module } from '@nestjs/common';
import { ConfigModule, ConfigService } from '@nestjs/config';
import { TypeOrmModule } from '@nestjs/typeorm';
import { AuthModule } from './auth/auth.module';
import { UsersModule } from './users/users.module';
import { AnimalsModule } from './animals/animals.module';
import { NoseModule } from './nose/nose.module';
import { EventsModule } from './events/events.module';
import { ClaimsModule } from './claims/claims.module';
import { AdminModule } from './admin/admin.module';

@Module({
  imports: [
    // 配置模块（加载 .env）
    ConfigModule.forRoot({
      isGlobal: true,
      envFilePath: '.env',
    }),

    // TypeORM 连接
    TypeOrmModule.forRootAsync({
      imports: [ConfigModule],
      inject: [ConfigService],
      useFactory: (config: ConfigService) => ({
        type: 'mysql',
        host:     config.get('DB_HOST'),
        port:     config.get('DB_PORT'),
        username: config.get('DB_USER'),
        password: config.get('DB_PASSWORD'),
        database: config.get('DB_NAME'),
        entities: [__dirname + '/**/*.entity{.ts,.js}'],
        synchronize: false,  // 生产环境必须 false，用 migration
        logging: false,
      }),
    }),

    AuthModule,
    UsersModule,
    AnimalsModule,
    NoseModule,
    EventsModule,
    ClaimsModule,
    AdminModule,
  ],
})
export class AppModule {}
```

---

## 四、公共模块（common/）

### 4.1 统一响应格式拦截器 `common/interceptors/transform.interceptor.ts`

所有接口统一返回 `{ code, message, data }`，由拦截器自动包装：

```typescript
import { Injectable, NestInterceptor, ExecutionContext, CallHandler } from '@nestjs/common';
import { Observable } from 'rxjs';
import { map } from 'rxjs/operators';

@Injectable()
export class TransformInterceptor implements NestInterceptor {
  intercept(context: ExecutionContext, next: CallHandler): Observable<any> {
    return next.handle().pipe(
      map((data) => ({
        code: 0,
        message: 'success',
        data: data !== undefined ? data : null,
      }))
    );
  }
}
```

### 4.2 JWT 守卫 `common/guards/jwt-auth.guard.ts`

```typescript
import { Injectable, ExecutionContext } from '@nestjs/common';
import { AuthGuard } from '@nestjs/passport';

@Injectable()
export class JwtAuthGuard extends AuthGuard('jwt') {}
```

### 4.3 角色守卫 `common/guards/roles.guard.ts`

```typescript
import { Injectable, CanActivate, ExecutionContext } from '@nestjs/common';
import { Reflector } from '@nestjs/core';
import { ROLES_KEY } from '../decorators/roles.decorator';

@Injectable()
export class RolesGuard implements CanActivate {
  constructor(private reflector: Reflector) {}

  canActivate(context: ExecutionContext): boolean {
    const requiredRoles = this.reflector.getAllAndOverride<string[]>(ROLES_KEY, [
      context.getHandler(),
      context.getClass(),
    ]);
    if (!requiredRoles) return true;
    const { user } = context.switchToHttp().getRequest();
    return requiredRoles.includes(user.role);
  }
}
```

### 4.4 角色装饰器 `common/decorators/roles.decorator.ts`

```typescript
import { SetMetadata } from '@nestjs/common';

export const ROLES_KEY = 'roles';
export const Roles = (...roles: string[]) => SetMetadata(ROLES_KEY, roles);
```

### 4.5 全局异常过滤器 `common/filters/http-exception.filter.ts`

```typescript
import { ExceptionFilter, Catch, ArgumentsHost, HttpException } from '@nestjs/common';
import { Response } from 'express';

@Catch()
export class HttpExceptionFilter implements ExceptionFilter {
  catch(exception: unknown, host: ArgumentsHost) {
    const ctx = host.switchToHttp();
    const res = ctx.getResponse<Response>();

    let status = 500;
    let message = '服务器内部错误';

    if (exception instanceof HttpException) {
      status = exception.getStatus();
      const body = exception.getResponse();
      message = typeof body === 'string' ? body : (body as any).message || message;
    }

    res.status(status).json({
      code: status,
      message,
      data: null,
    });
  }
}
```

---

## 五、各模块示例（以 Animals 为例）

### 5.1 Entity `animals/entities/animal.entity.ts`

```typescript
import { Entity, PrimaryColumn, Column, CreateDateColumn, UpdateDateColumn, OneToMany } from 'typeorm';

@Entity('animals')
export class Animal {
  @PrimaryColumn({ type: 'varchar', length: 36 })
  animal_id: string;

  @Column({ type: 'enum', enum: ['lost', 'found', 'claimed', 'archived'], default: 'lost' })
  status: string;

  @Column({ type: 'enum', enum: ['cat', 'dog', 'other'] })
  species: string;

  @Column({ type: 'varchar', length: 50, nullable: true })
  breed: string;

  @Column({ type: 'varchar', length: 50, nullable: true })
  color: string;

  @Column({ type: 'enum', enum: ['male', 'female', 'unknown'], default: 'unknown' })
  gender: string;

  @Column({ type: 'enum', enum: ['puppy', 'adult', 'senior'], nullable: true })
  age_estimate: string;

  @Column({ type: 'enum', enum: ['healthy', 'injured', 'ill', 'unknown'], default: 'unknown' })
  health_status: string;

  @Column({ type: 'boolean', default: false })
  sterilized: boolean;

  @Column({ type: 'datetime', name: 'first_seen_at' })
  first_seen_at: Date;

  @Column({ type: 'datetime', name: 'last_seen_at' })
  last_seen_at: Date;

  @Column({ type: 'decimal', precision: 10, scale: 8, name: 'location_lat' })
  location_lat: number;

  @Column({ type: 'decimal', precision: 11, scale: 8, name: 'location_lng' })
  location_lng: number;

  @Column({ type: 'varchar', length: 255, nullable: true })
  address: string;

  @Column({ type: 'text', nullable: true })
  notes: string;

  @Column({ type: 'json', nullable: true })
  tags: string[];

  @Column({ type: 'varchar', length: 36, nullable: true, name: 'primary_nose_id' })
  primary_nose_id: string;

  @Column({ type: 'json', nullable: true })
  photos: string[];

  @CreateDateColumn({ name: 'created_at' })
  created_at: Date;

  @UpdateDateColumn({ name: 'updated_at' })
  updated_at: Date;
}
```

### 5.2 DTO `animals/dto/query-animal.dto.ts`

```typescript
import { ApiPropertyOptional } from '@nestjs/swagger';
import { IsOptional, IsString, IsNumber, IsEnum, Min } from 'class-validator';
import { Type } from 'class-transformer';
import { Transform } from 'class-transformer';

export class QueryAnimalDto {
  @ApiPropertyOptional({ example: 1 })
  @IsOptional()
  @Type(() => Number)
  @IsNumber()
  @Min(1)
  page?: number = 1;

  @ApiPropertyOptional({ example: 20 })
  @IsOptional()
  @Type(() => Number)
  @IsNumber()
  limit?: number = 20;

  @ApiPropertyOptional({ example: 'dog' })
  @IsOptional()
  @IsEnum(['cat', 'dog', 'other'])
  species?: string;

  @ApiPropertyOptional({ example: 'lost' })
  @IsOptional()
  @IsEnum(['lost', 'found', 'claimed', 'archived'])
  status?: string;

  @ApiPropertyOptional({ example: '柴犬' })
  @IsOptional()
  @IsString()
  keyword?: string;
}
```

### 5.3 Controller `animals/animals.controller.ts`

```typescript
import { Controller, Get, Post, Put, Delete, Body, Param, Query, UseGuards } from '@nestjs/common';
import { ApiTags, ApiBearerAuth, ApiOperation } from '@nestjs/swagger';
import { JwtAuthGuard } from '../common/guards/jwt-auth.guard';
import { AnimalsService } from './animals.service';
import { QueryAnimalDto } from './dto/query-animal.dto';

@ApiTags('动物档案')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard)
@Controller('animals')
export class AnimalsController {
  constructor(private readonly animalsService: AnimalsService) {}

  @Get()
  @ApiOperation({ summary: '获取动物列表' })
  findAll(@Query() query: QueryAnimalDto) {
    return this.animalsService.findAll(query);
  }

  @Get(':id')
  @ApiOperation({ summary: '获取动物详情' })
  findOne(@Param('id') id: string) {
    return this.animalsService.findOne(id);
  }

  @Post()
  @ApiOperation({ summary: '新建动物档案（管理端）' })
  create(@Body() createDto: any) { // 详细 DTO 参照接口文档
    return this.animalsService.create(createDto);
  }

  @Put(':id')
  @ApiOperation({ summary: '更新动物档案' })
  update(@Param('id') id: string, @Body() updateDto: any) {
    return this.animalsService.update(id, updateDto);
  }

  @Delete(':id')
  @ApiOperation({ summary: '删除动物档案' })
  remove(@Param('id') id: string) {
    return this.animalsService.remove(id);
  }
}
```

### 5.4 Service `animals/animals.service.ts`

```typescript
import { Injectable, NotFoundException } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { Animal } from './entities/animal.entity';
import { QueryAnimalDto } from './dto/query-animal.dto';

@Injectable()
export class AnimalsService {
  constructor(
    @InjectRepository(Animal)
    private readonly animalRepo: Repository<Animal>,
  ) {}

  async findAll(query: QueryAnimalDto) {
    const { page = 1, limit = 20, species, status, keyword } = query;
    const qb = this.animalRepo.createQueryBuilder('a');

    if (species) qb.andWhere('a.species = :species', { species });
    if (status)  qb.andWhere('a.status = :status', { status });
    if (keyword) {
      qb.andWhere(
        '(a.breed LIKE :kw OR a.color LIKE :kw OR a.address LIKE :kw)',
        { kw: `%${keyword}%` }
      );
    }

    const [list, total] = await qb
      .orderBy('a.created_at', 'DESC')
      .skip((page - 1) * limit)
      .take(limit)
      .getManyAndCount();

    return { total, list };
  }

  async findOne(id: string) {
    const animal = await this.animalRepo.findOne({ where: { animal_id: id } });
    if (!animal) throw new NotFoundException('动物不存在');
    return animal;
  }

  async create(dto: Partial<Animal>) {
    const animal = this.animalRepo.create(dto);
    return this.animalRepo.save(animal);
  }

  async update(id: string, dto: Partial<Animal>) {
    const animal = await this.findOne(id);
    Object.assign(animal, dto);
    return this.animalRepo.save(animal);
  }

  async remove(id: string) {
    await this.findOne(id);
    await this.animalRepo.delete({ animal_id: id });
  }
}
```

### 5.5 Module `animals/animals.module.ts`

```typescript
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { AnimalsController } from './animals.controller';
import { AnimalsService } from './animals.service';
import { Animal } from './entities/animal.entity';

@Module({
  imports: [TypeOrmModule.forFeature([Animal])],
  controllers: [AnimalsController],
  providers: [AnimalsService],
  exports: [AnimalsService],
})
export class AnimalsModule {}
```

---

## 六、Auth 模块（登录/注册）

### 6.1 JWT Strategy `auth/strategies/jwt.strategy.ts`

```typescript
import { Injectable } from '@nestjs/common';
import { PassportStrategy } from '@nestjs/passport';
import { ExtractJwt, Strategy } from 'passport-jwt';
import { ConfigService } from '@nestjs/config';

@Injectable()
export class JwtStrategy extends PassportStrategy(Strategy) {
  constructor(config: ConfigService) {
    super({
      jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),
      ignoreExpiration: false,
      secretOrKey: config.get('JWT_SECRET'),
    });
  }

  async validate(payload: { user_id: string; role: string }) {
    return { user_id: payload.user_id, role: payload.role };
  }
}
```

### 6.2 Auth Service（简化版）

```typescript
// POST /api/auth/login → 返回 { token, user }
// POST /api/auth/register → 返回 { token, user }
// 详细实现参考接口文档字段要求
```

---

## 七、Admin 模块（管理端）

```typescript
// 所有路由加 @Roles('admin') + RolesGuard
// 路由示例
@Get('stats')
@ApiOperation({ summary: '管理后台统计数据' })
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles('admin')
getStats() { ... }
```

---

## 八、启动步骤

```bash
cd /mnt/f/swcup2026/backend

# 1. 建库建表（参考 docs/数据库建表脚本.md）
mysql -u root -p nose_rescue < /mnt/f/swcup2026/docs/数据库建表脚本.md

# 2. 安装依赖
npm install

# 3. 配置 .env（DB_PASSWORD 等）

# 4. 启动开发服务器
npm run start:dev

# 5. 访问 API 文档
# http://localhost:3000/api-docs
```

---

## 九、NestJS 带来的比赛加分点

| 加分点 | 说明 |
|--------|------|
| Swagger 自动文档 | 评委可直接在 `/api-docs` 体验完整 API |
| TypeScript 类型安全 | 接口字段类型自动校验，减少联调 bug |
| 模块化架构 | 代码结构清晰，评委眼前一亮 |
| 统一响应格式 | 全局拦截器保证所有接口格式一致 |
| 依赖注入 | Service 可测试性强 |
| DTO + class-validator | 请求参数自动校验，错误信息友好 |

---

## 十、当前状态

| 项目 | 状态 |
|------|------|
| NestJS 项目骨架 | ✅ 完整目录 + 核心配置代码 |
| 数据库建表脚本 | ✅ `docs/数据库建表脚本.md` |
| 真实接口逻辑 | ⬜ 按模块开发，参考接口文档 |
| AI 服务对接 | ⬜ 等模型就绪（`nose.service.ts` 中预留调用位） |
| Swagger 文档 | ✅ 启动后访问 `/api-docs` 即可查看 |
