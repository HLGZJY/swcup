# 前端项目结构 — 鼻纹智救

> 记录 `miniapp-user` 和 `miniapp-admin` 两个项目的实际目录结构。
> 本文档由代码同步维护，每次新增页面后更新。

---

## 1. 项目总览

```
swcup2026/
├── miniapp-user/           # 用户端小程序（普通救助者）
│   └── src/
└── miniapp-admin/          # 管理端小程序（管理员）
    └── src/
```

---

## 2. miniapp-user 目录结构

```
miniapp-user/src/
├── App.vue                  # 应用根组件（全局样式 + lang="scss"）
├── main.ts                  # Vue3 入口
├── manifest.json            # 小程序配置（AppID: __UNI__F204D0E）
├── pages.json               # 页面路由 + TabBar 配置
├── uni.scss                 # 全局 SCSS 变量（$uni-primary=#0FBF9F）
├── env.d.ts                 # TypeScript 模块声明
├── vite.config.ts           # Vite 构建配置（含 scss additionalData）
├── tsconfig.json
│
├── pages/
│   ├── index/index.vue          # 首页：动物列表 + 筛选
│   ├── login/index.vue           # 登录页：微信头像选择 + 昵称
│   ├── collect/
│   │   ├── index.vue             # 鼻纹采集引导（3步）
│   │   └── result.vue            # 比对结果（四维度得分 + Top-N）
│   ├── animal-detail/index.vue    # 动物详情
│   ├── claim/index.vue            # 认领申请
│   └── user/index.vue            # 个人中心
│
├── components/
│   ├── animal-card/index.vue         # 动物卡片
│   ├── nose-guide-box/index.vue      # 采集引导框
│   ├── fusion-score-panel/index.vue   # 融合得分面板
│   └── match-result-card/index.vue   # 比对结果卡片
│
├── services/
│   └── mock.js                 # Mock 数据 + 模拟接口函数
│
└── static/
    ├── mock/
    │   ├── avatar-default.svg
    │   ├── dog-placeholder.svg
    │   └── map-placeholder.svg   # （cat-placeholder.svg 待补充）
    ├── tab-home.png
    ├── tab-home-active.png
    ├── tab-user.png
    └── tab-user-active.png
```

---

## 3. miniapp-admin 目录结构

```
miniapp-admin/src/
├── App.vue                  # 应用根组件（全局样式 + lang="scss"）
├── main.ts                  # Vue3 入口
├── manifest.json            # 小程序配置（AppID: __UNI__A204D0F）
├── pages.json               # 页面路由 + TabBar 配置
├── uni.scss                 # 全局 SCSS 变量（$uni-primary=#FF6B6B）
├── env.d.ts
├── vite.config.ts           # Vite 构建配置（含 scss additionalData）
└── tsconfig.json
│
├── pages/
│   ├── admin/
│   │   ├── index.vue            # 管理首页：统计 + 待办
│   │   └── audit/index.vue      # 审核中心：事件/认领审核
│   ├── animals/index.vue         # 动物档案管理
│   ├── events/index.vue          # 事件管理
│   └── users/index.vue          # 用户管理
│
├── components/
│   ├── audit-event-card/index.vue   # 事件审核卡片
│   ├── audit-claim-card/index.vue    # 认领审核卡片（待创建）
│   ├── stat-card/index.vue          # 统计数字卡片
│   └── admin-table/index.vue         # 通用列表（待创建）
│
├── services/
│   └── mock.js                 # Mock 数据
│
└── static/
    ├── tab-home.png
    ├── tab-home-active.png
    ├── tab-animal.png
    ├── tab-animal-active.png
    ├── tab-event.png
    ├── tab-event-active.png
    ├── tab-user.png
    └── tab-user-active.png
```

---

## 4. 页面路由映射

### 4.1 用户端（miniapp-user）

| 页面路径 | 页面名称 | 启动方式 |
|---------|---------|---------|
| `pages/index/index` | 首页 | TabBar 首页 |
| `pages/login/index` | 登录 | 首页自动跳转（无 token 时） |
| `pages/collect/index` | 鼻纹采集 | 首页悬浮按钮 |
| `pages/collect/result` | 比对结果 | 采集完成后跳转 |
| `pages/animal-detail/index` | 动物详情 | 首页列表点击 |
| `pages/claim/index` | 认领申请 | 动物详情页底部按钮 |
| `pages/user/index` | 个人中心 | TabBar 我的 |

### 4.2 管理端（miniapp-admin）

| 页面路径 | 页面名称 | 启动方式 |
|---------|---------|---------|
| `pages/admin/index` | 管理首页 | TabBar 首页 |
| `pages/admin/audit/index` | 审核中心 | 管理首页待办入口 |
| `pages/animals/index` | 动物档案 | TabBar 动物 |
| `pages/events/index` | 事件管理 | TabBar 事件 |
| `pages/users/index` | 用户管理 | TabBar 用户 |

---

## 5. 组件清单

### 5.1 用户端组件

| 组件路径 | 用途 | 传入参数 | 状态 |
|---------|------|---------|------|
| `components/animal-card/index.vue` | 动物列表卡片 | `animal: Animal` | 已创建，未集成 |
| `components/nose-guide-box/index.vue` | 鼻纹采集引导框 | `species: string` | 已创建，未集成 |
| `components/fusion-score-panel/index.vue` | 四维度得分展示 | `result: CompareResult` | 已创建，未集成 |
| `components/match-result-card/index.vue` | 比对结果卡片 | `item: MatchItem` | 已创建，未集成 |

### 5.2 管理端组件

| 组件路径 | 用途 | 传入参数 | 状态 |
|---------|------|---------|------|
| `components/audit-event-card/index.vue` | 事件审核卡片 | `event: Event` | 已创建，未集成 |
| `components/audit-claim-card/index.vue` | 认领审核卡片 | `claim: Claim` | 待创建 |
| `components/stat-card/index.vue` | 统计数字卡片 | `value: number, label: string` | 已创建，未集成 |
| `components/admin-table/index.vue` | 通用列表 | `columns: [], data: []` | 待创建 |

---

## 6. 静态资源规范

### 6.1 TabBar 图标尺寸
- 81 × 81 px（微信小程序要求）

### 6.2 Mock 占位图
- 使用 SVG 内联（可缩放，不占体积）
- 真实图片上线前用占位图替代

### 6.3 主题色
- 管理端：`#FF6B6B`
- 用户端：`#0FBF9F`

---

## 7. 构建配置备忘

### 7.1 vite.config.ts 关键配置

```ts
// 两个项目均有此配置，确保 SCSS 变量全局可用
css: {
  preprocessorOptions: {
    scss: {
      additionalData: `@import "@/uni.scss";`
    }
  }
}
```

### 7.2 App.vue 样式语言

所有 `.vue` 文件的 `<style>` 块必须使用 `lang="scss"`，否则 SCSS 变量无法正确编译：
```vue
<style lang="scss">
```

---

## 8. 本文档维护规则

新增页面后，在此文档对应章节追加一行。组件同理。
