// 临时：vue-tsc 在本项目环境（TS 4.9.4 + vue-tsc 1.0.24 + @dcloudio/types 3.4.19）
// 下无法解析 .d.ts 的全局声明，shims 不生效。详见 HANDOFF.md。
//
// 真实编译验证走的是 uni build:mp-weixin —— 已通过。
// 本文件仅占位以避免影响 vite.config 的 include 模式。

export {}