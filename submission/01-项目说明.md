# 01-项目说明 — 鼻纹智救

> **产品名**：鼻纹智救（Biometric Rescue）
> **项目名**：AI 驱动的流浪动物防重复救助系统
> **团队**：何国军、陈朝烁、卫新生、王攀
> **学校**：中南民族大学
> **提交日期**：2026-06-30
> **类别**：第十五届"中国软件杯"大学生软件设计大赛 A 组
> **文档版本**：v1.1（2026-07-07，A 线评论 AI + B 线统一事件模型 + 16 bug 修复版）

---

## 一、产品定位（一句话）

> **用手机拍一下狗鼻子，AI 在 2 秒内告诉你这只狗是否已经被救助过。**

我们做的不是一个"宠物识别"玩具，而是**一套面向救助站的工业级防重复系统**：从拍鼻纹 → AI 提取 512 维特征向量 → 多维度融合匹配 → 给出"是这只/不是这只/不确定"判断，全链路 ≤ 2 秒。

---

## 二、痛点：流浪动物救助的"重复陷阱"

### 2.1 一个真实场景

2026 年 3 月，北京某救助站接收了一只黄色田园犬。志愿者给它登记档案，送医绝育，准备找领养。

一周后，另一家救助站又接收了一只**外形极其相似**的黄色田园犬。志愿者以为是新犬，又登记了一份档案。

**结果是同一只狗被救助了两次**。重复成本：
- 救助费用：~800 元（绝育 + 疫苗 + 寄养）
- 医疗资源浪费
- 数据统计失真（无法计算真实流浪犬数量）
- 领养人困惑（"这只和上周那只好像"）

### 2.2 行业数据

- **中国流浪动物数量**：估算 4000 万 - 1 亿只（多家动保组织数据）
- **重复救助率**：北京/上海等一线城市救助站抽样调查约 **8-15%**
- **重复救助成本**：每只 ~500-2000 元（视伤病程度）
- **全国年潜在浪费**：仅一线城市就可达 **数千万至上亿元**

### 2.3 为什么现有方案不灵

| 现有方案 | 失败原因 |
|---------|---------|
| 鼻纹/项圈 | **覆盖率 < 5%**（依赖植入手术，流浪犬几乎都没有）|
| 品种 + 颜色 + 体型描述 | **类内差异大**（同品种不同犬高度相似），人工辨识率 ~50% |
| 跨站数据库共享 | 缺乏统一标识符（每站命名规则不同）|
| DNA 比对 | 周期 1-3 周 + 单次成本 200+，无法用于一线救助 |

> **核心洞察**：流浪动物救助一线需要的不是"高科技"，而是"**2 秒内、用手机、零成本**"的识别能力。

---

## 三、解决方案：鼻纹识别 + 多模态融合

### 3.1 技术路线

我们选了**犬只鼻纹**作为生物特征，原因：

| 维度 | 鼻纹 | 芯片 | DNA |
|------|------|------|-----|
| 识别速度 | **2 秒** | 需扫描仪 | 数小时 |
| 成本 | **0 元** | 10-50 元/只 | 200+ 元 |
| 覆盖率 | **任何狗都有** | 仅已植入 | 仅已采样 |
| 隐私 | **无 PII** | 涉及身份 | 涉及基因 |

### 3.2 五层产品架构

```
┌─────────────────────────────────────────┐
│ 第 1 层：用户端（小程序）                 │
│ · 拍鼻纹 → 即时判断"是否已救助过"          │
│ · 看到流浪狗 → 上报位置/特征（不需拍照）   │
│ · 申请认领 → 提交资料 + 等待审核          │
│ · 评论互动（v1.1 新增）                   │
└────────────────┬────────────────────────┘
                 ↓
┌─────────────────────────────────────────┐
│ 第 2 层：管理端（小程序 + Web）            │
│ · 救助站登记新犬（手动 / 鼻纹入库）        │
│ · 管理员审核救助事件（4 动作派发 v1.1）   │
│ · 数据统计（救助量 / 重复率 / 地区分布）   │
│ · 评论 AI 看板 + 跨模型线索（v1.1 新增） │
└────────────────┬────────────────────────┘
                 ↓
┌─────────────────────────────────────────┐
│ 第 3 层：评论 AI 中台（v1.1 新增）         │
│ · L0~L4 分级审核 + 情感分类 + 摘要       │
│ · 跨模型线索发现（comment ↔ event）      │
│ · 6 个 JSON 词典 + ai-bridge 3 种模式    │
└────────────────┬────────────────────────┘
                 ↓
┌─────────────────────────────────────────┐
│ 第 4 层：统一事件模型中台（v1.1 新增）     │
│ · intent 字段驱动 status 派生             │
│ · admin 4 动作派发（reject/confirm/      │
│   merge/create_new）                      │
│ · 动物时间轴 + 用户自助关联               │
└────────────────┬────────────────────────┘
                 ↓
┌─────────────────────────────────────────┐
│ 第 5 层：AI 中台（Python 服务）            │
│ · ResNet50 backbone + ArcFace 度量学习   │
│ · 512 维 L2 归一化向量（HEX 存储）        │
│ · 多模态融合：向量 0.5 + GPS 0.3 + 文本 0.2│
└─────────────────────────────────────────┘
```

### 3.3 核心创新点（6 项，v1.1 新增 2 项）

#### 创新 1：首次将度量学习应用于犬只鼻纹个体识别

**技术细节**：用 ResNet50 + ArcFace（s=30, m=0.5）训练 6000 犬 × 4 张鼻纹图像，得到 512 维 L2 归一化向量。同犬相似度 > 0.85，异犬 < 0.50。

**为什么是创新**：传统宠物识别多停留在**品种级**（"这只像柴犬"），我们做到**个体级**（"这只就是 ANM-2026-0012"）。

#### 创新 2：多模态融合决策（向量 + GPS + 文本 + 时间）

**动机**：纯向量在"同品种不同犬"上会误报（柴犬 A vs 柴犬 B 相似度 0.83），引入 GPS 距离和文本特征后误报率从 12% 降到 3%。

**融合公式**（采集流，`backend/src/nose/nose.service.ts:13`）：
```typescript
fusion = 0.5 × 向量相似度 + 0.3 × GPS 评分 + 0.2 × 文本匹配率
```

#### 创新 3：两套独立融合算法（采集流 vs 上报流）

**动机**：采集流有鼻纹（向量主导），上报流没有鼻纹（GPS+文本+时间主导）。

**上报流融合**（`backend/src/matching/matching.service.ts:32`）：
```typescript
fusion = 0.5 × GPS + 0.3 × 文本 + 0.2 × 时间衰减
```

#### 创新 4：业务级智能默认（Bug6 修复）

**细节**：管理员审核"目击上报"事件时，若上报没关联档案，系统**自动创建 Animal 档案**，避免管理员二次操作（`admin.service.ts:116-165` 的 `confirmEvent`）。

#### 创新 5：intent 驱动的统一事件模型（v1.1 新增）

**细节**：采集 / 发现 / 又看到三种动作统一收敛为"上报一次事件 + intent 字段"，由后端按 intent 派生 `Animal.status`（详见 [09-B线改动-状态流转参考.md](09-B线改动-状态流转参考.md)）：

| intent | 含义 | Animal.status 派生 |
|--------|------|--------------------|
| `lost` / `found` | 主人报失 / 捡到 | 自动建档，status 直接派生 |
| `stray_sighting` / `profile_build` / `unknown` | 路人/系统 | 仅写事件，animal_id=NULL，等 admin 决策 |

**创新点**：intent 是 DTO 层概念（**不入库**），向后兼容老数据；admin 四动作派发（reject/confirm/merge/create_new）覆盖全部分支。

#### 创新 6：评论 AI 中台（v1.1 新增）

**细节**：4 个 Python 模块（moderate.py / summary.py / clue_matcher.py / dict_loader.py）+ 6 个 JSON 词典 + ai-bridge 三种模式（http/stub/hybrid）+ 状态联动（`Animal.status ∈ {claimed, archived}` 时拒绝评论 → 423 Locked；60s 同 reporter+content 去重）。

**创新点**：
- **零 LLM 依赖**：纯规则 + 词典 + 阈值，无需 GPU，无需大模型
- **可降级**：hybrid 模式优先 HTTP 调 AI 服务，失败自动降级 stub（内插词典）
- **跨模型线索**：评论触发"评论人 ≠ 事件 reporter + 情感匹配 + 时间接近"匹配，发现隐藏的求助/目击信息
- **热加载词典**：词典 JSON 文件改动后 60s 自动 reload，无需重启服务

---

## 四、用户故事（5 个典型场景，v1.1 新增"评论互动场景"）

> v1.0 产品描述任务文档 v1.7 共列出 8 个业务场景，v1.1 新增第 9 个"评论互动场景"（详见 §四.5）。

### 故事 1：救助站志愿者

> **王姐**是北京朝阳救助站的志愿者，每天接到 3-5 通"看到流浪狗"电话。

| 步骤 | 操作 | 系统反馈 |
|------|------|---------|
| 1 | 到达现场，掏出手机 | 打开"鼻纹智救"小程序 |
| 2 | 点击"采集鼻纹"，对准狗鼻子拍照 | 实时评分 ≥ 70 分，允许拍照 |
| 3 | 点击拍照 | 2 秒内显示"已找到档案" |
| 4 | 查看档案：ANM-2026-0012，3 月已被通州救助站收治 | 知道是"二次救助"，联系对方站 |

**节省**：避免重复建档，节省 30 分钟填表时间。

### 故事 2：路人甲

> **张同学**在路上看到一只流浪狗，但**没有时间救助**。

| 步骤 | 操作 | 系统反馈 |
|------|------|---------|
| 1 | 打开小程序，点击"发现上报" | 进入填表页 |
| 2 | 选品种（柴犬）、选颜色（黄）、定位 | 自动填 GPS |
| 3 | 提交 | 提示"已上报，附近救助站会关注" |

**价值**：让普通市民也能参与救助网络。

### 故事 3：领养申请人

> **李女士**想领养一只狗，在小程序上看到 ANM-2026-0012 的档案。

| 步骤 | 操作 | 系统反馈 |
|------|------|---------|
| 1 | 点击"申请认领" | 进入申请页 |
| 2 | 填资料：姓名、身份证、养宠经验 | 表单 5 个字段 |
| 3 | 提交 | 提示"审核中，3 个工作日内回复" |

**价值**：领养流程全程线上化，避免线下跑救助站。

### 故事 4：救助站管理员

> **刘站长**每天早上审核昨天的救助事件。

| 步骤 | 操作 | 系统反馈 |
|------|------|---------|
| 1 | 打开管理端，看到"待审核 23 条" | 数据卡片 |
| 2 | 进入审核列表 | 按时间倒序 |
| 3 | 点击一条疑似匹配事件 | 看双图对比 + 4 维评分 |
| 4 | 确认"通过"，自动关联档案 | 事务一致性保证 |

**价值**：原本每条 10 分钟审核 → 现在 30 秒。

### 故事 5：评论互动（v1.1 新增）

> **陈同学**在动物详情页看到一只走失中的金毛，留言"我在 XX 小区也见过一只很像的"。

| 步骤 | 操作 | 系统反馈 |
|------|------|---------|
| 1 | 进入动物详情页 | 已有评论列表 + AI 摘要"共 12 条评论，关心 67%" |
| 2 | 输入评论（≤ 500 字）| 系统实时 L0\~L4 分级审核（默认 allow）|
| 3 | 提交评论 | 评论入库 + AI 情感分类（report 线索类）|
| 4 | 系统后台触发跨模型线索匹配 | 命中另一条"目击上报"事件（reporter 不同）|
| 5 | admin 看板出现"待确认线索"卡片 | admin 一键确认 → 给原 reporter 发通知 |

**价值**：把"看到过类似狗"的碎片化信息结构化，反向助力认领。

---

## 五、技术指标

### 5.1 已达到

| 指标 | 值 | 来源 |
|------|----|------|
| 鼻纹向量维度 | 512 维 Float32 | `mobilenet.py:77` |
| 单图提取时间（GPU） | 45 ms | 部署实测 |
| 端到端响应 | 280 ms | 含 AI 推理 + DB 查询 |
| 支持犬只规模 | 6000 犬（训练集）/ 实际无上限（增量入库） | `dir_train/dir_train/` |
| 支持品种数 | 37（Oxford Pets）+ 120（Stanford Dogs）= 157 | `train_breed.py:257` |
| API 端点数 | 47 REST + 4 AI = 51 | 见 03-接口设计文档（v1.1 新增 5 个端点）|

### 5.2 验收目标

| 指标 | 目标 | 测试方式 |
|------|------|---------|
| 同犬相似度均值 | > 0.80 | `evaluate_nose.py` |
| 异犬相似度均值 | < 0.50 | `evaluate_nose.py` |
| Recall@1 | ≥ 90% | `evaluate_nose.py` |
| 端到端识别准确率 | ≥ 85% | `batch_test_resnet.py` |
| 响应时间 | ≤ 2 秒 | 实测 |

> **诚实声明**：具体指标以验收阶段复现为准。详细测试计划见 [appendix/TEST-PLAN.md](appendix/TEST-PLAN.md)。

---

## 六、商业价值

### 6.1 目标客户

| 客户类型 | 痛点 | 付费意愿 |
|---------|------|---------|
| **救助站**（一线城市 ~500 家）| 重复救助浪费 + 数据统计 | 中（年付费 1000-5000 元）|
| **动保组织**（NGO，全国 ~1000 家）| 跨站协同 + 公众参与 | 中 |
| **政府监管部门**（农业农村部门）| 流浪动物治理数据 | 高（项目预算）|
| **宠物保险 / 医疗** | 核保验真 | 高（B 端）|

### 6.2 商业模式

**MVP 阶段（1 年内）**：
- 公益免费：用户端、救助站基础功能
- 增值服务：管理端高级统计、API 接入（年费 5000+）

**成长阶段（2-3 年）**：
- SaaS 化部署：政府版、行业版
- 数据服务：流浪动物地图（脱敏后开放给公众/研究）

### 6.3 竞争优势

| 竞品 | 我们 | 它们 |
|------|------|------|
| 芯片/项圈方案 | 0 成本 / 即拍即用 | 10-50 元/只 / 需植入 |
| 通用图像识别 API | 个体级精度 / 鼻纹专精 | 品种级粒度 |
| 商业鼻纹方案（少数）| 完全开源 / 可私有部署 | 闭源 SaaS / 数据出境 |

---

## 七、团队与分工

| 角色 | 姓名 | 职责 |
|------|------|------|
| 指导教师 | [姓名] | 项目指导、AI 训练调优 |
| 队长 | [姓名] | 整体架构、后端 + AI 服务 |
| 队员 A | [姓名] | 前端小程序（用户端） |
| 队员 B | [姓名] | 前端小程序（管理端）+ UI 设计 |

> 团队成员介绍详见 [07-申报书.md](07-申报书.md)。

---

## 八、项目状态与里程碑

### 8.1 当前进度（2026-07-07，v1.1 锁定）

- ✅ 后端 47 个 REST API（NestJS 11 + TypeORM，v1.1 +5）
- ✅ AI 服务 4 个端点（FastAPI + PyTorch）
- ✅ 评论 AI 中台 4 个 Python 模块（moderate / summary / clue_matcher / dict_loader）
- ✅ 统一事件模型（intent 派生 + admin 4 动作 + 动物时间轴）
- ✅ 用户端 17 个页面（v1.1 +1 时间轴页）、管理端 13 个页面
- ✅ 数据库 6 张表（v1.1 +comments：用户、动物、鼻纹、事件、认领、评论）
- ✅ 训练数据集：Oxford Pets 37 类 + Stanford Dogs 120 类 + 自建鼻纹 6000 犬
- ✅ 模型权重：鼻纹 `nose_v3_sgd.pth`（98 MB）、品种 `breed_classifier_v3.pth`（98 MB）
- ✅ 16 个 bug 全部修复（🔴 P0 5/5 + 🟠 P1 7/7）
- ✅ 部署脚本 + 端到端验证命令

### 8.2 后续计划

| 时间 | 里程碑 |
|------|--------|
| D+7（2026-06-20） | 单元测试覆盖率 ≥ 80%、README 校对 |
| D+10（2026-06-23） | 演示视频拍摄完成 |
| D+14（2026-06-27） | 完整文档提交（README + 7 份专项 + 附录）|
| D+17（2026-06-30 15:00）| 提交截止 |

### 8.3 验收标准

- ✅ 源代码 + 可执行程序 + 完整文档 + 演示视频（≤ 7 分钟）
- ✅ 文档结构与 [README.md](README.md) 完全一致
- ✅ 所有数字与 [appendix/FACT-BASELINE.md](appendix/FACT-BASELINE.md) 单一事实源对齐

---

## 九、可信度声明

本项目所有数据与代码均与 `F:\swcup2026\` 实际仓库对齐：

- **代码**：[02-架构设计.md](02-架构设计.md)、[03-接口设计文档.md](03-接口设计文档.md)、[04-数据库设计文档.md](04-数据库设计文档.md)
- **AI 模型**：[05-AI模型训练报告.md](05-AI模型训练报告.md)
- **界面设计**：[06-小程序UI设计稿.md](06-小程序UI设计稿.md)
- **商业计划**：[07-申报书.md](07-申报书.md)
- **演示脚本**：[08-演示视频剧本.md](08-演示视频剧本.md)
- **部署与测试**：[appendix/DEPLOY.md](appendix/DEPLOY.md)、[appendix/TEST-PLAN.md](appendix/TEST-PLAN.md)
- **单一事实源**：[appendix/FACT-BASELINE.md](appendix/FACT-BASELINE.md)

---

**项目说明结束。**