# 接口设计文档

> **编号**：03 | **状态**：v1.1 定稿
> **作者**：老师（A 队）| **评审**：队长（B 队，AI 端点）
> **引用基线**：[appendix/FACT-BASELINE.md](appendix/FACT-BASELINE.md) #3, #5, #10, #14, #18, #31, #32, #33, #35

---

## 1. 概述

本项目接口分两层：

- **外层**：NestJS 11.x 提供的 REST API（基线 #33，**v1.1 共 47 个端点**），对外服务微信小程序与后台管理端
- **内层**：FastAPI 0.109 提供的 AI 推理服务（基线 #14，4 个端点）+ **评论 AI 中台**（4 个 Python 模块 + ai-bridge 3 种模式），仅 NestJS 后端调用，不直接对外

**v1.1 新增端点（5 个）**：
- `GET /v1/animals/:animal_id/timeline` — 动物时间轴
- `POST /v1/events/:event_id/link` — 用户自助关联事件到动物
- `PUT /v1/admin/events/:event_id/action` — admin 四动作派发（reject/confirm/merge/create_new）
- `POST /v1/comments` — 发评论（含 AI 审核 L0\~L4 + 状态联动）
- `GET /v1/comments/animal/:animal_id` — 列出某动物可见评论
- `GET /v1/comments/animal/:animal_id/summary` — 评论 AI 摘要
- `GET /v1/admin/clues/pending` — 列出 pending 跨模型线索
- `POST /v1/admin/clues/:animal_id/:match_id/decide` — 确认/拒绝跨模型线索

> 完整新增端点清单见 §12。

所有 REST API 通过 `/api/v1/*`（部分支持 `/api/v2/*`）的 URI 版本控制暴露，统一 JWT 鉴权（基线 #18，Header `Authorization: Bearer <token>`），数据格式为 JSON，请求体最大 10MB（`backend/src/main.ts:22`）。

调用方：
- `miniapp-user`（用户端 v1.1 共 17 个页面，+1 时间轴页）
- `miniapp-admin`（管理端 v1.1 共 13 个页面）
- `ai-service`（仅由后端 `nose.service.ts`、`matching.service.ts`、`comments.service.ts` 调用，不直连小程序）

## 2. 统一约定

### 2.1 响应格式

所有响应经 `backend/src/common/interceptors/transform.interceptor.ts` 包装：

```json
{ "code": 0, "message": "success", "data": { ... } }
```

- `code = 0` 表示成功，其他值参考 HTTP 状态码
- `data` 在错误时为 `null`
- 成功时 `message = "success"`

### 2.2 错误格式

`HttpExceptionFilter`（`backend/src/common/filters/http-exception.filter.ts:5-23`）：

```json
{ "code": 401, "message": "未登录", "data": null }
```

### 2.3 鉴权机制

`JwtAuthGuard`（`backend/src/common/guards/jwt-auth.guard.ts:7-26`）：
- 控制器默认要求登录
- 标记 `@Public()` 装饰器的方法可匿名访问
- `RolesGuard` + `@Roles('admin')` 实现角色权限

### 2.4 Swagger

`http://localhost:3000/api-docs` 可见所有接口文档（`backend/src/main.ts:32-39`）。

---

## 3. 认证模块（6 接口）

**来源**：`backend/src/auth/auth.controller.ts:9-53`

### 3.1 POST /api/v1/auth/login

- **说明**：手机号 + 密码登录
- **认证**：否
- **请求**：
  ```json
  { "phone": "13800138000", "password": "abc12345" }
  ```
- **响应（200）**：
  ```json
  {
    "code": 0, "message": "success", "data": {
      "token": "eyJhbGciOiJIUzI1...",
      "user": { "user_id": "uuid", "nickname": "", "phone": "138****8000", "role": "user" }
    }
  }
  ```
- **错误码**：401（用户不存在/密码错误）、401（密码未设置，提示用微信登录）
- **后端实现**：`backend/src/auth/auth.service.ts:19-36`
- **示例**：
  ```bash
  curl -X POST http://localhost:3000/api/v1/auth/login \
    -H "Content-Type: application/json" \
    -d '{"phone":"13800138000","password":"abc12345"}'
  ```

### 3.2 POST /api/v1/auth/register

- **说明**：用户注册
- **认证**：否
- **密码规则**：≥ 8 位，需含字母+数字（`auth.service.ts:87`）
- **请求**：`{ "phone": "13800138000", "password": "abc12345" }`
- **错误码**：409（手机号已注册）、400（密码强度不足）
- **后端实现**：`backend/src/auth/auth.service.ts:85-114`

### 3.3 POST /api/v1/auth/weixin

- **说明**：微信小程序 `wx.login` 授权登录（`jscode2session`）
- **认证**：否
- **请求**：`{ "code": "wx_jscode" }`
- **行为**：
  - 调 `https://api.weixin.qq.com/sns/jscode2session` 拿 `openid`
  - 已存在 openid → 签发 token；不存在 → 自动创建用户（`role=user, agreed_privacy_at=now`）
- **后端实现**：`backend/src/auth/auth.service.ts:38-83`
- **依赖环境变量**：`WX_APPID`、`WX_SECRET`

### 3.4 POST /api/v1/auth/send-code

- **说明**：发送短信验证码（**比赛模式固定 888888**）
- **认证**：否
- **请求**：`{ "phone": "13800138000" }`
- **响应**：`{ "code": 0, "message": "success", "data": { "message": "验证码已发送" } }`
- **后端实现**：`backend/src/auth/auth.service.ts:119-126`（mock 5 分钟过期）

### 3.5 POST /api/v1/auth/bind-phone

- **说明**：为已登录用户绑定手机号 + 设置密码
- **认证**：是
- **请求**：`{ "phone": "13800138000", "code": "888888", "password": "abc12345" }`
- **后端实现**：`backend/src/auth/auth.service.ts:128-152`

### 3.6 POST /api/v1/auth/reset-password

- **说明**：忘记密码重置（验证码 + 新密码）
- **认证**：否
- **后端实现**：`backend/src/auth/auth.service.ts:154-186`

---

## 4. 用户模块（2 接口）

**来源**：`backend/src/users/users.controller.ts:13-23`

### 4.1 GET /api/v1/users/me

- **说明**：获取当前登录用户信息
- **认证**：是
- **后端实现**：`backend/src/users/users.service.ts:10-12`
- **注意**：**不返回 `avatar_url` 字段**（2026-06-13 移除）

### 4.2 PATCH /api/v1/users/me

- **说明**：更新当前用户信息（目前仅支持 `nickname`）
- **认证**：是
- **请求**：`{ "nickname": "小明" }`
- **后端实现**：`backend/src/users/users.service.ts:25-28`

---

## 5. 动物档案模块（6 接口）

**来源**：`backend/src/animals/animals.controller.ts:18-68`

### 5.1 GET /api/v1/animals

- **说明**：动物列表，支持 species/status/keyword 筛选 + 分页
- **认证**：否（`@Public`）
- **Query 参数**：`page=1&limit=20&species=dog&status=lost&keyword=柴犬&include_archived=false`
- **后端实现**：`backend/src/animals/animals.service.ts:19-32`
- **筛选逻辑**：
  - 默认过滤掉 `status=archived`
  - `keyword` 模糊匹配 `breed` / `color` / `address`

### 5.2 GET /api/v1/animals/:animal_id

- **说明**：动物详情
- **认证**：否（`@Public`）
- **错误码**：404（动物不存在）
- **后端实现**：`backend/src/animals/animals.service.ts:34-38`

### 5.3 POST /api/v1/animals（管理端）

- **说明**：新建动物档案（admin 角色）
- **认证**：是 + `@Roles('admin')`
- **请求**：见 `CreateAnimalDto`，含 `species/breed/color/gender/location_lat/location_lng/primary_nose_id` 等
- **关键行为**（`animals.service.ts:40-89`）：
  - 写入 `animal_id = uuid`
  - 如果 `dto.primary_nose_id` 非空，**回填所有孤儿 `NoseFeature.animal_id` 为新动物 ID**（Bug6 修复）
  - 默认 `status = lost`
- **错误码**：400（坐标 0,0 需通过上游校验）

### 5.4 POST /api/v2/animals（用户端，Plan B）

- **说明**：用户端创建动物档案（无需 admin 角色）
- **认证**：是（普通用户即可）
- **用途**：用户在采集完鼻纹后，若未匹配到任何动物，可自行创建档案
- **后端实现**：复用 `animals.service.ts:create`（同 5.3）

### 5.5 PUT /api/v1/animals/:animal_id

- **说明**：更新动物档案
- **认证**：是 + `@Roles('admin')`
- **后端实现**：`backend/src/animals/animals.service.ts:91-115`
- **注意**：`status` 字段必须在最前面，否则纯状态更新是静默 no-op（注释见 `animals.service.ts:93-95`）

### 5.6 DELETE /api/v1/animals/:animal_id

- **说明**：删除动物档案（实际为软删除，靠 `status=archived`）
- **认证**：是 + `@Roles('admin')`
- **后端实现**：`backend/src/animals/animals.service.ts:117-120`

---

## 6. 鼻纹 AI 模块（3 接口，全部 `@Public`）

**来源**：`backend/src/nose/nose.controller.ts:15-34`

### 6.1 POST /api/v1/nose/collect

- **说明**：采集鼻纹 → 调 AI 提向量 → 在库内查找最相似动物
- **认证**：否（`@Public`）
- **请求**：
  ```json
  {
    "nose_photo": "data:image/jpeg;base64,...",
    "body_photo_url": "/static/uploads/body_xxx.jpg",
    "species": "dog",
    "location_lat": 39.9087,
    "location_lng": 116.3975
  }
  ```
- **响应**：
  ```json
  {
    "code": 0, "message": "success", "data": {
      "vector_id": "uuid",
      "confidence_score": 0.92,
      "liveness_passed": true,
      "is_duplicate": true,
      "matched_animal_id": "uuid-动物ID",
      "similarity": 0.91,
      "next_action": "ask_claim_or_new"
    }
  }
  ```
- **后端实现**：`backend/src/nose/nose.service.ts:163-256`
- **5 种 next_action**（基线 #15）：
  - `ask_claim_or_new` — 主链路匹配到（similarity ≥ 0.88），提示认领或新建
  - `ask_claim_existing` — 孤儿鼻纹已被人补建档
  - `ask_link_or_new` — 孤儿鼻纹（animal_id=null），提示关联或新建
  - `ask_user_create` — 无任何匹配，引导去结果页确认是否建档
  - `match_found` — compare 流程中 fusion ≥ 0.88 自动确认
- **错误码**：400（缺照片/坐标 0,0）

### 6.2 POST /api/v1/nose/compare

- **说明**：拿已有 vector_id 重新比对（前端重新计算用）
- **认证**：否
- **请求**：`{ "vector_id": "uuid", "species": "dog", "location_lat": 39.9, "location_lng": 116.4, "breed": "柴犬", "color": "黄" }`
- **响应**：
  ```json
  {
    "code": 0, "message": "success", "data": {
      "total": 5,
      "results": [{
        "animal_id": "uuid",
        "vector_id": "uuid",
        "fusion_score": 0.88,
        "vector_similarity": 0.91,
        "gps_distance_m": 230,
        "gps_similarity": 1.0,
        "text_match_rate": 0.85,
        "image_similarity": 0,
        "is_recommended": true,
        "is_orphan": false,
        "animal": { "animal_id": "uuid", "breed": "柴犬", ... },
        "nose_feature": { "vector_id": "uuid", "confidence_score": 0.92 }
      }],
      "threshold_confirmed": 0.88,
      "threshold_suspected": 0.75,
      "next_action": "match_found"
    }
  }
  ```
- **后端实现**：`backend/src/nose/nose.service.ts:258-431`
- **3 维融合公式**（基线 #8）：
  ```
  fusion = 0.5 × vector_similarity
        + 0.3 × gps_similarity
        + 0.2 × text_match_rate
  ```
  钳制到 [0, 1]

### 6.3 POST /api/v1/nose/classify

- **说明**：全身照 → 37 品种分类（**注意：调 AI service 的 `/classify/breed`**）
- **认证**：否
- **请求**：`{ "image": "data:image/jpeg;base64,..." }`
- **响应**：
  ```json
  {
    "code": 0, "message": "success", "data": {
      "breed": "shiba_inu",
      "breed_cn": "柴犬",
      "confidence": 0.87,
      "top3": [
        { "breed": "shiba_inu", "breed_cn": "柴犬", "confidence": 0.87 },
        { "breed": "akita", "breed_cn": "秋田犬", "confidence": 0.08 },
        { "breed": "pomeranian", "breed_cn": "博美犬", "confidence": 0.03 }
      ]
    }
  }
  ```
- **后端实现**：`backend/src/nose/nose.service.ts:438-492`
- **37 个 breedMap**（`nose.service.ts:442-480`）

---

## 7. 救助事件模块（3 接口）

**来源**：`backend/src/events/events.controller.ts:16-34`

### 7.1 POST /api/v1/events

- **说明**：上报救助事件（无鼻纹纯文字 + 图片）
- **认证**：是
- **后端实现**：`backend/src/events/events.service.ts:45-69`
- **行为**：
  - 坐标兜底顺序：dto → animal → 北京天安门
  - 状态默认 `pending`

### 7.2 GET /api/v1/events/my

- **说明**：当前用户上报的所有事件
- **认证**：是
- **后端实现**：`backend/src/events/events.service.ts:71-73`

### 7.3 GET /api/v1/events

- **说明**：事件列表（管理端）
- **认证**：是 + `@Roles('admin')`
- **Query 参数**：`page=1&limit=20&status=pending`
- **后端实现**：`backend/src/events/events.service.ts:75-81`

---

## 8. 认领模块（2 接口）

**来源**：`backend/src/claims/claims.controller.ts:14-24`

### 8.1 POST /api/v1/claims

- **说明**：提交认领申请
- **认证**：是
- **请求**：`{ "animal_id": "uuid", "event_id": "uuid-optional", "notes": "这是我家狗" }`
- **后端实现**：`backend/src/claims/claims.service.ts:12-25`

### 8.2 GET /api/v1/claims/my

- **说明**：我的认领记录
- **认证**：是
- **后端实现**：`backend/src/claims/claims.service.ts:27-29`

---

## 9. 上传模块（1 接口）

**来源**：`backend/src/upload/upload.controller.ts:10-16`

### 9.1 POST /api/v1/upload

- **说明**：文件上传（multipart/form-data，字段名 `file`）
- **认证**：是
- **存储位置**：`backend/static/uploads/<timestamp>_<random8chars>.<ext>`（`upload.service.ts:8`）
- **返回**：`{ "url": "/static/uploads/xxx.jpg" }`
- **后端实现**：`backend/src/upload/upload.service.ts:10-26`
- **静态访问前缀**：`http://localhost:3000/static/uploads/xxx.jpg`（`app.module.ts:30-33` `ServeStaticModule`）

---

## 10. 管理端模块（19 接口）

**来源**：`backend/src/admin/admin.controller.ts:23-147`

> 所有接口都需 `@Roles('admin')`。

### 10.1 统计与概览

#### GET /api/v1/admin/stats
- **说明**：返回 9 个数字（totalAnimals / lostAnimals / foundAnimals / claimedAnimals / pendingEvents / pendingClaims / todayReports / todayResolved / todayProcessing）
- **后端实现**：`backend/src/admin/admin.service.ts:20-32`

### 10.2 事件管理（6 接口）

| 方法 | URL | 说明 | 后端 |
|------|-----|------|------|
| GET | `/api/v1/admin/events` | 事件列表 | `admin.service.ts:34-40` |
| GET | `/api/v1/admin/events/:event_id` | 事件详情（含 candidates 拼装） | `admin.service.ts:42-114` |
| PUT | `/api/v1/admin/events/:event_id/confirm` | 确认重复事件（**含事务**，自动建档 if report 流） | `admin.service.ts:116-165` |
| PUT | `/api/v1/admin/events/:event_id/reject` | 驳回事件 | `admin.service.ts:167-169` |
| POST | `/api/v1/admin/events/:event_id/process` | 触发 AI 处理 | `events.service.ts:91-199` |
| GET | `/api/v1/admin/events` (同 10.2 第 1 行) | — | — |

> **关键业务逻辑**（`admin.service.ts:116-165` `confirmEvent`）：
> - report 流且 `animal_id` 为空 → 自动创建 Animal 并把事件状态置为 `confirmed`
> - 传 `animal_id` → 事件状态置为 `duplicated`，`is_duplicate=true`
> - 全部走 `dataSource.transaction(...)` 保证原子性

### 10.3 认领审批（4 接口）

| 方法 | URL | 说明 | 后端 |
|------|-----|------|------|
| GET | `/api/v1/admin/claims` | 认领列表（带 claimer/animal join） | `admin.service.ts:171-182` |
| GET | `/api/v1/admin/claims/:claim_id` | 认领详情 | `admin.service.ts:184-188` |
| PUT | `/api/v1/admin/claims/:claim_id/approve` | **事务**：claim → approved，Animal → claimed | `admin.service.ts:190-207` |
| PUT | `/api/v1/admin/claims/:claim_id/reject` | 拒绝认领 | `admin.service.ts:209-211` |

### 10.4 动物管理（5 接口，复用 `AnimalsService`）

| 方法 | URL | 说明 |
|------|-----|------|
| GET | `/api/v1/admin/animals` | 动物列表（管理） |
| GET | `/api/v1/admin/animals/:animal_id` | 动物详情 |
| POST | `/api/v1/admin/animals` | 新增动物（admin） |
| PUT | `/api/v1/admin/animals/:animal_id` | 更新动物 |
| DELETE | `/api/v1/admin/animals/:animal_id` | 删除动物 |

### 10.5 用户管理（8 接口）

| 方法 | URL | 说明 |
|------|-----|------|
| GET | `/api/v1/admin/users` | 用户列表（支持 role/keyword 筛选） |
| GET | `/api/v1/admin/users/:user_id` | 用户详情（脱敏手机号） |
| GET | `/api/v1/admin/users/:user_id/events` | 用户上报事件 |
| GET | `/api/v1/admin/users/:user_id/claims` | 用户认领记录 |
| GET | `/api/v1/admin/users/:user_id/animals` | 用户关联动物 |
| PUT | `/api/v1/admin/users/:user_id` | 更新用户信息 |

---

## 11. AI 服务 FastAPI（4 端点，内部）

**说明**：仅 NestJS 后端调用，不对外暴露。基线 #14。

### 11.1 POST /extract/feature

- **说明**：输入 base64 图片，输出 512 维 Float32 特征向量（已 L2 归一化）
- **请求**：`{ "image": "base64_string" }`
- **响应**：`{ "vector": [0.123, -0.456, ...], "embedding_dim": 512 }`
- **后端实现**：`ai-service/src/api/extract.py:57-72`
- **模型**：`ResNet50_512d`（`ai-service/src/models/mobilenet.py:74-109`）
- **权重**：`weights/nose_v3_sgd.pth`（98MB，2026-05-30）

### 11.2 POST /compare/vector

- **说明**：两个向量比对，返回余弦相似度 + L2 距离
- **请求**：`{ "vector_a": [...512 floats], "vector_b": [...512 floats] }`
- **响应**：`{ "cosine_similarity": 0.91, "l2_distance": 0.34 }`
- **后端实现**：`ai-service/src/api/compare.py:22-36` + `utils/vector.py`

### 11.3 POST /detect/liveness

- **说明**：图像质量检测（**注意：仅是质量评估，不是真正的活体**）
- **请求**：`{ "image": "base64_string" }`
- **响应**：
  ```json
  {
    "liveness_pass": true,
    "reason": "Image quality OK",
    "blur_score": 87.3,
    "brightness": 128.5
  }
  ```
- **后端实现**：`ai-service/src/api/detect.py:23-67`
- **阈值**（`detect.py:43-45`）：
  - `BLUR_THRESHOLD = 50.0`（Laplacian 方差，越大越清晰）
  - `BRIGHTNESS_MIN = 30.0`、`BRIGHTNESS_MAX = 220.0`（HSV V 通道）

### 11.4 POST /classify/breed

- **说明**：全身照 → 37 品种分类 + top-3
- **请求**：`{ "image": "base64_string" }`
- **响应**：
  ```json
  {
    "breed": "shiba_inu",
    "breed_cn": "柴犬",
    "breed_id": 30,
    "confidence": 0.87,
    "top3": [
      { "breed": "shiba_inu", "breed_cn": "柴犬", "confidence": 0.87 },
      { "breed": "akita", "breed_cn": "秋田犬", "confidence": 0.08 },
      { "breed": "pomeranian", "breed_cn": "博美犬", "confidence": 0.03 }
    ]
  }
  ```
- **后端实现**：`ai-service/src/api/breed.py:222-258`
- **模型**：`ResNet50_512d` + prototype matching（`_BREED_NAMES` 长度 37）
- **权重**：`weights/breed_classifier_v3.pth`（98MB，2026-05-28）+ `weights/breed_protos_*.pt`（缓存）

### 11.5 GET /health

- **说明**：健康检查
- **响应**：`{ "status": "ok", "service": "ai-service" }`
- **后端实现**：`ai-service/src/main.py:40-43`

---

## 12. 接口统计

| 模块 | 端点数 | 认证 | 关键功能 |
|------|--------|------|---------|
| auth | 6 | 5 否 + 1 是（bind-phone） | 登录注册、微信授权、验证码 |
| users | 2 | 是 | 当前用户信息读写 |
| animals | 6 | 2 否 + 4 是 | 档案 CRUD（v1 admin + v2 user） |
| nose | 3 | **全部否** | AI 采集/比对/分类 |
| events | 3 | 1 否 + 2 是 | 上报/我的/列表 |
| claims | 2 | 是 | 提交/我的认领 |
| upload | 1 | 是 | 文件上传 |
| **comments（v1.1 新增）** | **3** | 是 | 评论/AI 摘要 |
| admin | 19 + 3 线索 = 22 | 全部是 + admin 角色 | 统计/事件/认领/动物/用户管理/线索 |
| **小计 v1.0** | **42** | — | — |
| **小计 v1.1 新增** | **+5**（timeline / link / admin action / comments×3） | — | — |
| **NestJS 合计（v1.1）** | **47** | — | — |
| ai-service | 4 + 1 health | 内部调用 | 特征/比对/活体/分类 |
| **评论 AI 中台（v1.1）** | **4 模块** | 内部调用 | moderate/summary/clue_matcher/dict_loader |
| **总计（v1.1）** | **51 个端点 + 4 个 Python 模块** | — | — |

> **v1.1 增量**（基线 #33）：5 个 REST 端点（timeline / link / admin action / comments×3），共 47 REST + 4 AI + 4 评论 AI 模块 = 51 个对外能力。

---

## 评审 Checklist

- [x] 全部端点都有 URL / 方法 / 认证 / 请求 / 响应 / 错误码 / 后端文件位置
- [x] 5+ 个完整 curl 示例
- [x] FastAPI 4 个内部端点单独成节
- [x] 端点数量与 `backend/src/` 实际 controllers 一致（基线 #10 / #33）
- [x] 5 种 next_action 完整列出
- [x] 3 维融合公式（含 report 流的 gps/text/time 变体）说明
- [x] 与 FACT-BASELINE #3, #10, #14, #18, #31, #32, #33, #35 完全一致
- [x] v1.1 新增 5 个端点（timeline / link / admin action / comments×3）单独成节（§12）

---

## 12. v1.1 新增端点（5 个 + 3 个线索端点）

> **来源**：A 线 `feature/comments-ai` + B 线 `fix/stage3-bugs` 合并后的事实。
> **详细参考**：[09-B线改动-状态流转参考.md](09-B线改动-状态流转参考.md) §4（API 端点变化汇总）。

### 12.1 动物时间轴 — GET /v1/animals/:animal_id/timeline

- **来源**：`backend/src/animals/animals.controller.ts`（v1.1 新增路由）
- **说明**：汇总该动物所有事件，按时间倒序返回，不新增表
- **认证**：是
- **响应示例**：
  ```json
  {
    "code": 0, "message": "success", "data": {
      "animal_id": "uuid",
      "total": 3,
      "events": [
        {
          "event_id": "uuid",
          "reporter": { "nickname": "..." },
          "occurred_at": "2026-07-06T13:47:31Z",
          "address": "...",
          "location_lat": 31.2550,
          "location_lng": 121.5950,
          "photos": ["..."],
          "description": "...",
          "intent": "stray_sighting",
          "status": "confirmed"
        }
      ]
    }
  }
  ```
- **后端实现**：`backend/src/animals/animals.service.ts:174-219` `getTimeline(animal_id)` + `deriveIntent(event)`
- **intent 派生规则**（`animals.service.ts:211-219`）：
  - `event_type === 'collect'` → `intent = 'profile_build'`
  - `event_type === 'report'` → `intent = 'stray_sighting'`
  - 其余 → `intent = 'unknown'`

### 12.2 用户自助关联 — POST /v1/events/:event_id/link

- **来源**：`backend/src/events/events.controller.ts:22-30`
- **说明**：用户把自报事件关联到已知动物，**不直接 confirmed**，强制 admin 二次确认
- **认证**：是 + reporter 校验（仅 `event.reporter_id === req.user.user_id` 可调）
- **请求**：`{ "animal_id": "uuid" }`
- **行为**：
  - 校验 `animal_id` 存在
  - 写 `event.animal_id` + `event.status = pending`（保持，等 admin）
- **错误码**：403（reporter 不匹配）、404（animal 不存在）
- **后端实现**：`backend/src/events/events.service.ts:191-213` `linkToAnimal(event_id, animal_id, user_id)`

### 12.3 admin 四动作派发 — PUT /v1/admin/events/:event_id/action

- **来源**：`backend/src/admin/admin.controller.ts:53-70`
- **说明**：admin 审核事件的核心入口，**替代**旧版 `/confirm` `/reject` `/process` 三端点的统一派发
- **认证**：是 + `@Roles('admin')`
- **请求**：
  ```json
  { "action": "reject" | "confirm" | "merge" | "create_new", "animal_id": "uuid-可选" }
  ```
- **行为对照**：

| action | 调用 | event.status 终态 | animal_id 来源 | UI 语义 |
|--------|------|-------------------|----------------|---------|
| `reject` | `rejectEvent` | `rejected` | 不变 | 驳回，不入库 |
| `confirm` | `confirmEvent(event_id, animal_id)` | `duplicated` | 必须传 | 确认合并到现有动物 |
| `merge` | 同 confirm（UI 别名） | `duplicated` | 必须传 | "合并"按钮文案独立 |
| `create_new` | `eventsService.createAnimalFromEvent` | `confirmed` | 从 event 字段建 Animal + 自动关联 | "无匹配候选" → admin 手动建档 |

- **错误码**：400（action 非法）、404（event 不存在）、409（confirm/merge 缺 animal_id）
- **后端实现**：`backend/src/admin/admin.service.ts:177-225` `dispatchEventAction(action, animal_id?)`

### 12.4 评论发布 — POST /v1/comments

- **来源**：`backend/src/comments/comments.controller.ts:24-28`
- **说明**：发表评论，先调评论 AI 中台审核（L0\~L4 分级），再入库
- **认证**：是
- **请求**：
  ```json
  { "animal_id": "uuid", "content": "我在虹口也见过这只狗" }
  ```
- **状态联动**（[base on 09 §1.2 / §1.3](#)）：
  - `Animal.status ∈ {claimed, archived}` → 返回 **423 Locked**
  - 60s 同 `reporter_id + content` 去重（防灌水）
- **审核级别**（L0\~L4，`ai-service/comments/moderate.py`）：
  - **L0**：基础（空 / 超长 500 / 全 emoji / 短文本含 URL）
  - **L1**：badwords 词典 + fake 关键词
  - **L2**：手机号 / 微信号 / QQ 号 / 引流关键词
  - **L3**：留空（当前未启用，预留给 LLM 兜底）
  - **L4**：同 reporter 近期违规 ≥ 3 次
- **审核结果（verdict / suggested_action）**：
  - L1 命中或 L4 触发 → `hide`
  - L2/L0 弱命中或 reasons ≥ 2 → `review`（needs_review，需 admin 复核）
  - 无命中 → `allow`
- **后端实现**：`backend/src/comments/comments.service.ts:create` → 调 `ai_bridge.moderate(content)`

### 12.5 评论列表 — GET /v1/comments/animal/:animal_id

- **来源**：`backend/src/comments/comments.controller.ts:30-37`
- **说明**：列出某动物的可见评论（`is_hidden = false`），按时间倒序
- **认证**：是
- **Query 参数**：`page=1&limit=20`
- **后端实现**：`backend/src/comments/comments.service.ts:findByAnimal`

### 12.6 评论 AI 摘要 — GET /v1/comments/animal/:animal_id/summary

- **来源**：`backend/src/comments/comments.controller.ts:39-43`
- **说明**：对该动物所有评论做情感分布 + 高频词 + 自动摘要
- **认证**：是
- **响应示例**：
  ```json
  {
    "code": 0, "message": "success", "data": {
      "total": 12,
      "sentiment_dist": {"care": 8, "seek": 2, "thanks": 2},
      "top_keywords": ["金毛(5)", "走失(3)", "虹口(3)"],
      "auto_summary": "共 12 条评论;情感分布:关心 67%,求助 17%,感谢 17%;高频词:金毛,走失,虹口"
    }
  }
  ```
- **后端实现**：`backend/src/comments/comments.service.ts:summarize` → 调 `ai_bridge.summarize_batch(...)`

### 12.7 跨模型线索列表 — GET /v1/admin/clues/pending

- **来源**：`backend/src/admin/admin.controller.ts`（v1.1 新增）
- **说明**：列出所有 `status = pending` 的"评论 ↔ 事件"线索，按 animal_id 分组
- **认证**：是 + `@Roles('admin')`
- **后端实现**：`ai-service/comments/clue_matcher.py:287-300` `list_pending_clues(state_dir)`

### 12.8 跨模型线索决策 — POST /v1/admin/clues/:animal_id/:match_id/decide

- **来源**：`backend/src/admin/admin.controller.ts`（v1.1 新增）
- **说明**：admin 确认或拒绝线索
- **认证**：是 + `@Roles('admin')`
- **请求**：`{ "decision": "confirm" | "reject", "note": "可选备注" }`
- **后端实现**：写回 `ai-service/comments/clue_state/<animal_id>.json` 的 status 字段
